# Production environment variables for GitHub Pages
# Set these as GitHub Actions secrets for automatic deployment
# Or manually add them to .env.production for local production builds
VITE_LIBSQL_URL=libsql://your-database-url.turso.io
VITE_LIBSQL_TOKEN=your-production-token-here
